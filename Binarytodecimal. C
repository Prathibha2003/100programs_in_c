#include <stdio.h>
int convert(int);
void main()
{
int dec, bin;
printf("Enter a binary number: ");
scanf("%d", &bin);
dec = convert(bin);
printf("The decimal equivalent of %d is %d.\n", bin,dec);
}
int convert(int bin)
{
if (bin == 0)
{
}
else
{
}
}
return 0;
return (bin % 10 + 2 * convert(bin / 10));
